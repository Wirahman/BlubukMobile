import 'package:flutter/material.dart';


class Pesan extends StatelessWidget {
  @override
  Widget build(BuildContext context) => new Center(
      child: new Text("Menu ini akan digunakan untuk pesan"),
    );

}